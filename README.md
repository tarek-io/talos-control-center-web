# Talos Hand Trackpad Web

Public web build of the Talos Hand trackpad interface.

The app is published with GitHub Pages and serves the current browser interface for the trackpad visualizer and gesture UI.

## Local file

- `index.html`
